# =============================================================================
# Hermes Agent — ~/.hermes/.env template
# DO NOT commit actual keys. Fill in after bare-metal restore.
# Copy to: ~/.hermes/.env
# =============================================================================

# --- Primary LLM provider ---
# GitHub Copilot (authenticate via: hermes auth)
# No key needed — OAuth via gh CLI

# --- Ollama (local, no key needed) ---
# Configured in config.yaml — points to http://localhost:11434/v1

# --- Optional cloud providers (uncomment and fill as needed) ---
# OPENROUTER_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GROQ_API_KEY=
